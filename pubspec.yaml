name: binary_search_uzimizniki
description: A starting point for Dart libraries or applications.
version: 1.0.0
repository: https://github.com/parvisss/binary-search-.git
homepage: https://github.com/parvisss/binary-search-.git

environment:
  sdk: ^3.3.2

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
license: MIT
